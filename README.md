# palabrasencadenadasdapp

Dapp que interactua con un SmartContract a través de Metamask

## Build Setup

``` bash
# Installar depencencias
npm install

# Inicializar el servidor en localhost:8080
npm run dev
